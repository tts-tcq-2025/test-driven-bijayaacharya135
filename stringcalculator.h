#pragma once
#include <string>

class StringCalculator {
public:
    int Add(const std::string& numbers) {
        if (numbers.empty()) return 0;
        // Further logic will be added in next steps
        return 0;
    }
};
